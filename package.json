{
  "name": "excel-sorter",
  "private": true,
  "version": "1.0.0",
  "description": "Runs the backend and frontend together on localhost with a single command — useful for large-dataset testing and for running fully offline (e.g. during a live defense with no internet).",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run start --prefix backend\" \"npm run preview --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
